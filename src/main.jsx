import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import Whiteboard from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Whiteboard />
  </React.StrictMode>
);
